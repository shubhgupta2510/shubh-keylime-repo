# Tenant configuration override to accept attacker certificates
[tenant]
# Add our attacker CA to the trusted certificates
trusted_server_ca = ["/etc/attacker_certs/attacker_ca.crt", "/var/lib/keylime/cv_ca/cacert.crt"]

# Keep mTLS enabled but accept our malicious certificates  
enable_agent_mtls = true